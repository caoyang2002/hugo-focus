# Hugo 多语言配置完全指南

多语言配置允许您为不同语言的用户提供本地化内容，主要应用于菜单、界面文本和内容翻译。以下是四种标准配置方法的详细说明。

## 📚 一、字典文件配置 (i18n)

### 文件结构
```
i18n/
├── zh.toml      # 中文翻译字典
├── en.toml      # 英文翻译字典
└── ja.toml      # 日文翻译字典（可选）
```

### 字典文件示例

**`i18n/zh.toml`**
```toml
[home]
other = "首页"

[about]
other = "关于我们"

[read_more]
other = "阅读全文"

[page_not_found]
other = "页面未找到"

[recent_posts]
other = "最新文章"

[search_placeholder]
other = "搜索文章..."

[footer_copyright]
other = "版权所有 © {{ now.Format \"2006\" }} {{ .Site.Title }}"
```

**`i18n/en.toml`**
```toml
[home]
other = "Home"

[about]
other = "About"

[read_more]
other = "Read More"

[page_not_found]
other = "Page Not Found"

[recent_posts]
other = "Recent Posts"

[search_placeholder]
other = "Search articles..."

[footer_copyright]
other = "Copyright © {{ now.Format \"2006\" }} {{ .Site.Title }}"
```

### 在模板中使用字典
```go-html-template
<!-- 基本用法 -->
<h2>{{ i18n "recent_posts" }}</h2>
<a href="{{ .RelPermalink }}">{{ i18n "read_more" }}</a>

<!-- 带参数的用法 -->
<p>{{ i18n "footer_copyright" }}</p>

<!-- 条件判断 -->
{{ if eq .Site.Language.Lang "zh" }}
  <span>中文内容</span>
{{ else }}
  <span>English Content</span>
{{ end }}
```

## ⚙️ 二、单一 hugo.toml 配置方法

### 完整配置示例
```toml
# ===== 网站基本配置 =====
baseURL = "https://example.org/"
title = "短松江月"
theme = "my-theme"
defaultContentLanguage = "zh"
defaultContentLanguageInSubdir = false
hasCJKLanguage = true
enableRobotsTXT = true
paginate = 10

# ===== 语言配置 =====
[languages]
  # 中文配置
  [languages.zh]
    languageCode = "zh-CN"
    languageDirection = "ltr"
    languageName = "中文"
    contentDir = "content/zh"
    weight = 1  # 排序权重
    
    [languages.zh.params]
      subtitle = "中文文档"
      description = "用中文记录技术与思考"
      keywords = "技术,博客,中文"
      author = "作者名"
      github = "https://github.com/username"
    
    # 中文菜单配置
    [languages.zh.menu]
      [[languages.zh.menu.main]]
        identifier = "home"
        name = "首页"
        url = "/"
        weight = 10
      
      [[languages.zh.menu.main]]
        identifier = "posts"
        name = "文章"
        url = "/posts/"
        weight = 20
      
      [[languages.zh.menu.main]]
        identifier = "tags"
        name = "标签"
        url = "/tags/"
        weight = 30
      
      [[languages.zh.menu.main]]
        identifier = "about"
        name = "关于"
        url = "/about/"
        weight = 40

  # 英文配置
  [languages.en]
    languageCode = "en-US"
    languageDirection = "ltr"
    languageName = "English"
    contentDir = "content/en"
    weight = 2
    
    [languages.en.params]
      subtitle = "English Documentation"
      description = "Recording technology and thoughts in English"
      keywords = "technology,blog,english"
      author = "Author Name"
      github = "https://github.com/username"
    
    # 英文菜单配置
    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "home"
        name = "Home"
        url = "/"
        weight = 10
      
      [[languages.en.menu.main]]
        identifier = "posts"
        name = "Posts"
        url = "/posts/"
        weight = 20
      
      [[languages.en.menu.main]]
        identifier = "tags"
        name = "Tags"
        url = "/tags/"
        weight = 30
      
      [[languages.en.menu.main]]
        identifier = "about"
        name = "About"
        url = "/about/"
        weight = 40

# ===== 构建配置 =====
[build]
  writeStats = true
  useResourceCacheWhen = "fallback"

# ===== 输出格式 =====
[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML"]
  section = ["HTML", "RSS"]
```

## 📁 三、使用 config 目录的配置方法

### 推荐的文件结构
```
config/
├── _default/
│   ├── hugo.toml      # 基础配置
│   ├── menus.toml     # 默认菜单
│   └── params.toml    # 默认参数
├── zh/
│   ├── menus.toml     # 中文菜单
│   └── params.toml    # 中文参数
├── en/
│   ├── menus.toml     # 英文菜单
│   └── params.toml    # 英文参数
└── production/        # 生产环境配置（可选）
    └── hugo.toml
```

### 配置文件示例

**`config/_default/hugo.toml`**（基础配置）
```toml
baseURL = "https://example.org/"
title = "短松江月"
theme = "my-theme"
defaultContentLanguage = "zh"
defaultContentLanguageInSubdir = false
hasCJKLanguage = true
paginate = 10

[languages]
  [languages.zh]
    languageCode = "zh-CN"
    languageName = "中文"
    weight = 1
  
  [languages.en]
    languageCode = "en-US"
    languageName = "English"
    weight = 2
```

**`config/zh/params.toml`**（中文参数）
```toml
[params]
  subtitle = "中文文档"
  description = "用中文记录技术与思考"
  keywords = "技术,博客,中文"
  author = "作者名"
  
  [params.social]
    github = "https://github.com/username"
    twitter = "https://twitter.com/username"
    email = "me@example.com"
```

**`config/en/params.toml`**（英文参数）
```toml
[params]
  subtitle = "English Documentation"
  description = "Recording technology and thoughts in English"
  keywords = "technology,blog,english"
  author = "Author Name"
  
  [params.social]
    github = "https://github.com/username"
    twitter = "https://twitter.com/username"
    email = "me@example.com"
```

## 🗂️ 四、使用独立菜单文件配置方法

### 文件结构（简化版）
```
config/
├── hugo.toml          # 主配置
├── menus.zh.toml      # 中文菜单
└── menus.en.toml      # 英文菜单
```

### 菜单文件示例

**`config/menus.zh.toml`**
```toml
# 中文主菜单
[[main]]
  identifier = "home"
  name = "首页"
  url = "/"
  weight = 10
  pre = "<i class='icon-home'></i>"

[[main]]
  identifier = "posts"
  name = "文章"
  url = "/posts/"
  weight = 20

[[main]]
  identifier = "categories"
  name = "分类"
  url = "/categories/"
  weight = 30

[[main]]
  identifier = "tags"
  name = "标签"
  url = "/tags/"
  weight = 40

[[main]]
  identifier = "about"
  name = "关于"
  url = "/about/"
  weight = 50

# 中文页脚菜单
[[footer]]
  identifier = "privacy"
  name = "隐私政策"
  url = "/privacy/"
  weight = 10

[[footer]]
  identifier = "terms"
  name = "使用条款"
  url = "/terms/"
  weight = 20
```

**`config/menus.en.toml`**
```toml
# English Main Menu
[[main]]
  identifier = "home"
  name = "Home"
  url = "/"
  weight = 10
  pre = "<i class='icon-home'></i>"

[[main]]
  identifier = "posts"
  name = "Posts"
  url = "/posts/"
  weight = 20

[[main]]
  identifier = "categories"
  name = "Categories"
  url = "/categories/"
  weight = 30

[[main]]
  identifier = "tags"
  name = "Tags"
  url = "/tags/"
  weight = 40

[[main]]
  identifier = "about"
  name = "About"
  url = "/about/"
  weight = 50

# English Footer Menu
[[footer]]
  identifier = "privacy"
  name = "Privacy Policy"
  url = "/privacy/"
  weight = 10

[[footer]]
  identifier = "terms"
  name = "Terms of Service"
  url = "/terms/"
  weight = 20
```

## 🔄 配置合并规则

Hugo 按以下优先级合并配置：
1. **最高优先级**：内存中的配置
2. **环境配置**：`config/production/` 或 `config/development/`
3. **语言配置**：`config/zh/`、`config/en/` 等
4. **默认配置**：`config/_default/`
5. **最低优先级**：根目录的 `hugo.toml`

## 🛠️ 实用命令与调试

```bash
# 1. 查看最终合并的配置
hugo config

# 2. 查看特定语言的配置
hugo config --lang zh

# 3. 验证配置语法
hugo config --logLevel info

# 4. 开发服务器（禁用快速渲染便于调试）
hugo server --disableFastRender

# 5. 构建网站
hugo --minify --cleanDestinationDir
```

## 📊 配置方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **单一文件** | 配置集中，易于查看 | 文件臃肿，维护困难 | 小型网站，语言少（1-2种） |
| **config目录** | 模块化，结构清晰 | 需要理解合并规则 | 中大型网站，多语言（3+种） |
| **独立菜单文件** | 菜单配置独立，易于管理 | 其他配置仍需主文件 | 菜单复杂的项目 |
| **混合模式** | 灵活，可按需组合 | 配置分散，需要规范 | 企业级复杂项目 |

## 💡 最佳实践建议

1. **从简单开始**：小型项目先用单一文件，复杂后再拆分
2. **保持一致性**：所有语言版本的 `identifier` 必须一致
3. **使用版本控制**：配置文件纳入 Git 管理
4. **文档化**：在项目 README 中记录配置结构
5. **测试验证**：每次修改后使用 `hugo config` 验证

## 🚨 常见错误及修复

| 错误现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 菜单不显示 | 1. 模板未调用菜单<br>2. 权重冲突 | 检查模板中的 `.Site.Menus.main` |
| 语言切换失效 | 翻译页面未关联 | 在 Front Matter 中添加 `translations` |
| 配置不生效 | 优先级问题 | 使用 `hugo config` 查看最终配置 |

## 总结

选择哪种配置方法取决于项目规模和复杂度：
- **个人博客**：单一 `hugo.toml` 文件
- **中型网站**：`config/_default/` + 语言目录
- **大型项目**：完整的 `config` 目录结构

关键是要理解 Hugo 的配置合并机制，确保配置的正确性和一致性。配置完成后，务必使用 `hugo config` 命令验证最终结果。
